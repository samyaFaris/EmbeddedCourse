#include<stdio.h>
#include"triangle.h"

int main(void)
{
   firstFunction('*', 3);
   secondFunction('*', 3);
   thiredFunction('*', 3);
   fun4('*', 5);
   fun5('*', 5);
 fun6('*', 5);

   return 0; 
}
