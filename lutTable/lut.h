
#include <stdio.h>


/**
 * Returns 1 if the  majority of the  bits are on, otherwise 0 .
 *input: number.
 */
int majority(unsigned int n);


/**
 * Returns the mirror of the  given number.
 *input: number.
 output: mirror of the number
 */
unsigned int bitMirror(unsigned int n);

/**
 * Returns the index of the third set bit (1) in the given number.
 * If the number has fewer than 3 set bits,returns 0.
 *input: number.
 *output: index of the third 1
 */

 unsigned int bite_three_index(unsigned int n);
